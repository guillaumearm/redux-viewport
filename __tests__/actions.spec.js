import { listenMedia, clearMedia, LISTEN_MEDIA, CLEAR_MEDIA, UPDATE_MEDIA } from 'src';
import { updateMedia } from 'src/actions'; // private

describe('redux-viewport', () => {
    describe('action types', () => {
        describe('LISTEN_MEDIA', () => {
            it('will be test soon', () => {});
        });
        describe('CLEAR_MEDIA', () => {
        });
        describe('UPDATE_MEDIA', () => {
        });
    })
    describe('action creators', () => {
        describe('listenMedia', () => {
        });
        describe('clearMedia', () => {
        });
        describe('updateMedia', () => {
        });
    })
});
